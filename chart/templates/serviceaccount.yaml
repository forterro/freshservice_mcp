{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "freshservice-mcp.serviceAccountName" . }}
  labels:
    {{- include "freshservice-mcp.labels" . | nindent 4 }}
{{- end }}
