apiVersion: v1
kind: Service
metadata:
  name: {{ include "freshservice-mcp.fullname" . }}
  labels:
    {{- include "freshservice-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "freshservice-mcp.selectorLabels" . | nindent 4 }}
